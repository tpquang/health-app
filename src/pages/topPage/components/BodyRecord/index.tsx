interface BodyRecordProps {
  data: any;
}

const BodyRecord = ({ data }: BodyRecordProps) => {
  if (!data) return null;

  return (
    <section >
      {/* Body record content */}
    </section>
  );
};

export default BodyRecord; 